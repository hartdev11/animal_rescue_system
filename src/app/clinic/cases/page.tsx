import { ClinicCasesList } from "@/components/clinic/clinic-cases-list";

export const metadata = {
  title: "เคสทั้งหมด | Clinic Portal",
};

export default function ClinicCasesPage() {
  return <ClinicCasesList />;
}
